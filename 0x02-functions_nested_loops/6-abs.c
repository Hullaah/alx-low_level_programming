#include "main.h"
/**
 * _abs - calculates the absolute value of a number
 * @a: operand
 * Return: absolute value of a number.
*/

int _abs(int a)
{

if (a < 0)
{
return (a * -1);
}
else
{
return (a);
}

}
