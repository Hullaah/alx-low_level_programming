#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int number = 48;

while (number < 58)
{
putchar(number);
if (number == 57);
else
{
putchar(44);
}
putchar(32);
number++;
}

putchar(10);
    
return (0);
}
